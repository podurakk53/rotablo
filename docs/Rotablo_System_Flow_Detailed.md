# Rotablo System Flow Detailed

**Status**: Deprecated reference path.

The previous content of this file has been archived to `docs/archive/Rotablo_System_Flow_Detailed_legacy.md`.

It described a different assumption set that conflicts with the current V1 rules, including GPS tracking, background location, heavy offline sync, and infrastructure that is not the active repo truth.

Use these files instead:

- `docs/Rotablo_V1_Locked_Decisions.md`
- `docs/Rotablo_Application_Architecture_v0.md`
- `context-pack/current-task.md`
- `backend/prisma/schema.prisma`

If a new detailed system flow is needed, rebuild it from the current scaffold and locked decisions instead of reviving the legacy version.
